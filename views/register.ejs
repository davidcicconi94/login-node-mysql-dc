<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Login</title>
</head>
<body style="background-color:rgb(28, 139, 139)">
   <section class="container">
       <div class="row content d-flex justify-content-center mt-5">
        <div class="col-md-5">
            <div class="box shadow bg-white p-4">
                <h3 class="mb-4 text-center fs-1">Register</h3>
                <form action="register" method="post" class="mb-3">
                    <div class="form-floating mb-3">
                        <input name="name" type="text" class="form-control rounded-0" placeholder="name" id="floatingName" autocomplete="off" required autofocus>
                        <label for="floatingName">Name</label>
                    </div>
                    <div class="form-floating mb-3">
                        <input name="user" type="email" class="form-control rounded-0" placeholder="name@example.com" id="floatingInput" required autocomplete="off">
                        <label for="floatingInput">Email</label>
                    </div>
                    <select name="rol" class="form-select mb-3" aria-label="Default select example">
                        <option selected>-Rol-</option>
                        <option value="admin">Admin</option>
                        <option value="data-entry">Data Entry</option>
                      </select>
                    <div class="form-floating mb-3">
                        <input name="pass" type="password" class="form-control rounded-0" placeholder="password" id="floatingPass" required>
                        <label for="floatingPass">Password</label>
                    </div>
                    <div class="d-grid gap-2 mb-3">
                        <button type="submit" class="btn btn-success">Save</button>
                    </div>
                    <div class="d-grid gap-2 mb-3">
                        <a href="login" type="button" class="btn btn-danger">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
       </div>
   </section>
   <!-- Sweet Alert 2 -->
   <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>

   <% if(typeof alert != "undefined") { %>
   <script>
       Swal.fire({
           title: '<%= alertTitle %>',
           text: '<%= alertMessage %>',
           icon: '<%= alertIcon %>',
           showConfirmButton: '<%= showConfirmButton %>',
           timer:' <%= timer %>'
       }).then(() => {
         window.location = '/<%= ruta %>'
       })
       </script>
       <% } %>
</body>
</html>